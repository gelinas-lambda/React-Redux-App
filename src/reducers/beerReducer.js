import {  } from '../actions'

const initialState = {
    test: 'test'
}

export const beerReducer = (state = initialState, action) => {
    switch(action.type){
        //case EXAMPLE:
        //case EXAMPLE:
        default:
            return state;
    }
}